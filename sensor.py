# handle moisture sensor things

def get_moisture():
    # get moisture level from the sensor and add it to active plant profile
    # add watering date -> when moisture level rises, new watering date. it needs to all the time check mositure level
    # maybe in main or somewhere: current moisture level - get new moisture level using this function - if new higher than old -> new watering date
    return

